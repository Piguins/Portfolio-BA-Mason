# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Environment variables
.env
.env.local
.env.production
# But allow .env.example to be committed (it's a template)
!.env.example

# Deployment files (không cần commit)
*.md.backup

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

.vercel

# Root level package files (monorepo - no package.json at root)
package-lock.json

# Scripts (optional - uncomment if you don't want to commit)
# install-node.sh

# Husky setup files (should be committed)
# .husky/ should be committed to share hooks with team
